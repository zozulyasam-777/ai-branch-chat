# Core dependencies for v0.1
# We keep it minimal to ensure easy installation

requests>=2.28.0        # For API calls (OpenAI, etc.) in future versions
rich>=13.0.0            # For beautiful CLI output (colors, tables)
